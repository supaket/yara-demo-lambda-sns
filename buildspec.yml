version: 0.1
phases:
  install:
    commands:
      - npm install -g serverless
  build:
    commands:
      - npm install
  post_build:
    commands:
      - serverless deploy --stage $env –verbose
